# Changelog

## 0.1.0-dev.1

- Initial development release: the driver session envelope (`hello`, `init`,
  `event`, `update`, `error`, `terminate`, heartbeat), its JSON codec, and the
  session state machine (ordering, legality, version agreement).
