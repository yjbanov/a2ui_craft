## 0.1.0-dev.1

Initial prerelease. Translates an A2UI catalog component tree and data model into
the RFW model rendered by the framework adapters. Framework-neutral.

Depends on a prerelease of `a2ui_core`.
